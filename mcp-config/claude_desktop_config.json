{
  "_comment": "Add this to your Claude Desktop MCP config to use the Perchance AI Toolkit with autonomous capabilities",
  "mcpServers": {
    "perchance": {
      "command": "npx",
      "args": ["perchance-mcp"],
      "env": {
        "PLAYWRIGHT_HEADLESS": "true"
      }
    }
  }
}
